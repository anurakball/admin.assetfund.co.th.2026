@using Microsoft.AspNetCore.Http
@using thaicredit_hr_admin.Areas.Admin.Helpers
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    var mod = (Module)ViewBag.Module;
    var mod_config = mod.Config;
    var _utility = (Utility)ViewBag._utility;
    var _admin = (AdminHelpers)ViewBag._admin;
    var _session = (ISession)ViewBag._session;
}

@section NavbarBack{
    @await Component.InvokeAsync("AdminNavbarBack", new { linkBack = string.Format("/Admin/{0}", mod.Name) })
}

@await Component.InvokeAsync("AdminBreadcrumb", new { Module = mod })

<script src="/js/spectrum.min.js"></script>
<link rel="stylesheet" type="text/css" href="/css/Admin/spectrum.min.css">

<div class="form-wrapper">
    <form method="post" id="fm_cms" onsubmit="return sConfirmCustom(this);" class="">
        @Html.AntiForgeryToken()
         
        <div class="row">
            @Html.Raw(_admin.InputText("หัวข้อ", "title", ""))
        </div>

        <br />
        @* --- ซ่อนชั่วคราว: สีพื้นหลัง ---
        <div class="row">
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputText("สีพื้นหลัง", "main_color", ""))
                <script>
                    $("#id_main_color").spectrum({
                        color: "#FFFFFF"
                    });
                </script>
            </div>
        </div>
        --- สิ้นสุด: สีพื้นหลัง --- *@
         
        <div class="row">
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputFileManeger("รูปภาพ <span style='font-weight: normal;color: orange;'>(.webp, .svg, .png, .jpg, .jpeg, .gif — ขนาดที่แนะนำ 1530 x 665 px)</span>", "img1", ""))
                <div class="mt-2" style="font-size:0.85rem;color:#6c757d;line-height:1.6;">
                    กรอบ Pop-up บนหน้าเว็บถูกล็อกไว้ที่สัดส่วน <strong>1530 x 665 px</strong> —
                    รูปที่สัดส่วนไม่ตรงจะถูกกางเต็มกรอบทั้งความกว้างและความสูง (ส่วนที่เกินจะถูกครอบตัด)
                    จึงควรอัปโหลดตามขนาดที่แนะนำเพื่อให้ภาพแสดงครบถ้วน<br />
                    หากเพิ่ม Pop-up มากกว่า 1 รายการ หน้าเว็บจะแสดงเป็นสไลด์เลื่อนอัตโนมัติ เรียงตามลำดับที่ตั้งไว้ (สูงสุด 10 รายการ)
                </div>
            </div>
        </div>

        <div class="row">
            @Html.Raw(_admin.InputTextLinkCMSPage("URL <span class='fi fi-th'></span>", "url", ""))
            @Html.Raw(_admin.InputTextLinkCMSPage("URL <span class='fi fi-us'></span>", "en_url", ""))
        </div>

        <br />

        <div class="row">
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputUrlTarget("URL Target", "url_target", ""))
            </div>
        </div>

        @* --- ซ่อนชั่วคราว: เนื้อหา ---
        <div class="row">
            @Html.Raw(_admin.InputTextEditor("เนื้อหา <span class='fi fi-th'></span>", "info", ""))
            @Html.Raw(_admin.InputTextEditor("เนื้อหา <span class='fi fi-us'></span>", "en_info", ""))
        </div>
        --- สิ้นสุด: เนื้อหา --- *@

        @* --- ซ่อนชั่วคราว: ปุ่มด้านล่าง 1,2,3 ---
        <label for="id_main_color" class="form-label">ปุ่มด้านล่าง</label>
        <table style="width:100%; background-color:#CCC;  border-spacing: 1px;border-collapse: separate;" cellpadding="30" cellspacing="1">
            <tr style="background-color:#FFF;">
                @{
                    for(int i=1;i<=3;i++)
                    {
                        <td>
                        <div class="row">
                        <h4>@Html.Raw("ปุ่มที่ "+i)</h4>
                        </div>
                        <br>
                        <div class="row">
                        @Html.Raw(_admin.InputText("ข้อความในปุ่ม", "title_" + i + "_text", ""))
                        </div>
                        <br />
                        <div class="row">
                        @Html.Raw(_admin.InputText("สีปุ่ม", "title_" + i + "_color", ""))
                        @Html.Raw("<script> $('#id_title_"+i+"_color').spectrum({ color: '#FFFFFF' }); </script>")
                        </div>
                        <br />
                        <div class="row">
                        @Html.Raw(_admin.InputText("สีข้อความ", "title_" + i + "_color_text", ""))
                        @Html.Raw("<script> $('#id_title_"+i+"_color_text').spectrum({ color: '#FFFFFF' }); </script>")
                        </div>
                        <br />
                        <div class="row">
                        @Html.Raw(_admin.InputTextLinkCMSPage("ลิ้งค์ URL ปุ่ม", "title_" + i + "_url", ""))
                        </div>
                        <br />
                        <div class="row">
                        @Html.Raw(_admin.InputUrlTarget("URL Target", "title_" + i + "_url_target", ""))
                        </div>
                        </td>
                    }
                }
            </tr>
        </table>
        --- สิ้นสุด: ปุ่มด้านล่าง 1,2,3 --- *@


        <br />
         
        <script>
            $(document).ready(function () { 
                $(":input[name='issue_date_config']").click(function () {
                    if ($(this).val() == '2') {
                        $('#div_id_issue_date').stop(true, false).slideDown();
                    }
                    else {
                        $('#div_id_issue_date').stop(true, false).slideUp();
                    }
                });
            });
        </script>
        <div class="row">
            <div class="col-lg-6 col-md-6 mb-3">
                <div class="field">
                    <label class="form-label">วันที่แสดงผล</label>
                    <div class="row">
                        <div class="col-md-6 col-sm-6 col-6">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="issue_date_config" id="id_issue_date_config_1" value="1" checked>
                                <label class="form-check-label" for="id_issue_date_config_1">
                                    แสดงตลอดเวลา
                                </label>
                            </div>
                        </div>
                        <div class="col-md-6 col-sm-6 col-6">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="issue_date_config" id="id_issue_date_config_2" value="2">
                                <label class="form-check-label" for="id_issue_date_config_2">
                                    กำหนดเอง
                                </label>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
         
        <div class="" id="div_id_issue_date" style="display:none;">
            <div class="row">
                <div class="col-lg-6 col-md-6 mb-3">
                    @Html.Raw(_admin.InputDateTime("วันที่แสดงผล", "issue_date", System.DateTime.Now.ToString("dd/MM/yyyy HH:mm")))
                </div>
                <div class="col-lg-6 col-md-6 mb-3">
                    @Html.Raw(_admin.InputDateTime("วันที่สิ้นสุดแสดงผล", "expiry_date", System.DateTime.Now.AddYears(1).ToString("dd/MM/yyyy HH:mm")))
                </div>
            </div>
        </div>

        @await Component.InvokeAsync("AdminBtnSave", new { linkClose = "/Admin/" + mod.Name })
    </form>
</div>
 
@await Component.InvokeAsync("LinkHelper")
 
@section Scripts{
<link rel="stylesheet" href="~/lib/flag-icons-main/css/flag-icons.min.css" />
<script>

    function sConfirmCustom(form) {

        if(document.getElementById('id_issue_date_config_2').checked == true)
        { 
            if (
                $('#id_issue_date').val() != '' && $('#id_expiry_date').val() != '' &&
                $('#id_issue_date').datetimepicker('getValue') > $('#id_expiry_date').datetimepicker('getValue')
            ) 
            {
                sAlert("วันที่ไม่ถูกต้อง", "วันที่สิ้นสุด ต้องมากกว่า วันที่แสดงผล", "warning");
                return false;
            }
        }
         
        // Validate required fields
        var titleField = $('[name="title"]');
        if (titleField.length > 0 && titleField.val().trim() === '') {
            Swal.fire({ icon: 'warning', title: 'กรุณากรอกข้อมูล', text: 'กรุณากรอก Title ก่อนบันทึก', confirmButtonText: 'ตกลง' });
            titleField.focus();
            return false;
        }
        var enTitleField = $('[name="en_title"]');
        if (enTitleField.length > 0 && enTitleField.val().trim() === '') {
            Swal.fire({ icon: 'warning', title: 'กรุณากรอกข้อมูล', text: 'กรุณากรอก EN Title ก่อนบันทึก', confirmButtonText: 'ตกลง' });
            enTitleField.focus();
            return false;
        }
        Swal.fire({
            title: 'ยืนยัน ?',
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#0047B6',
            cancelButtonColor: '#BC1320',
            confirmButtonText: 'ใช่',
            cancelButtonText: 'ปิด',
            showClass: {
                popup: 'animate__animated animate__bounceIn'
            },
            hideClass: {
                popup: 'animate__animated animate__bounceOut'
            }
        }).then((result) => {
            if (result.isConfirmed) {
                form.submit();
            } else {
                return false;
            }
        });
        return false;
    }

    //----- Add fn addHouse
    Date.prototype.addHours = function(h) {
        this.setTime(this.getTime() + (h*60*60*1000));
        return this;
    }
    Date.prototype.addMinutes = function(m) {
        this.setTime(this.getTime() + (m*60*1000));
        return this;
    }

    $(document).ready(function() {

        //$(":input[name='title'],:input[name='main_color']").prop("required", true);
             
        setTimeout(function(){
            $(':input[name="issue_date"]').datetimepicker("destroy");
            $(':input[name="issue_date"]').datetimepicker({
                format: 'd/m/Y H:i',
                minDate: 0,
                onClose: function(current_time, $input){
                    //console.log('input value', $input.datetimepicker('getValue'));
                    //console.log('new Date()', new Date().addHours(-1));
                    if ($input.val() != '' && $input.datetimepicker('getValue') < new Date().addHours(-1)) {
                        $input.val('');
                        sAlert("วันที่ไม่ถูกต้อง", "ไม่สามารถเลือกวันที่ น้อยวันที่ปัจจุบันได้", "warning");
                        //console.log('condition', ($input.val() != '' && $input.datetimepicker('getValue') < new Date().addHours(-1)));
                    }
                },
            });
        },500);
    });
</script>
}