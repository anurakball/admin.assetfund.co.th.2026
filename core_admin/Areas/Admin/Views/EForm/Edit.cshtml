@using Microsoft.AspNetCore.Http
@using thaicredit_hr_admin.Areas.Admin.Helpers
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    var mod = (Module)ViewBag.Module;
    var mod_config = mod.Config;
    var _utility = (Utility)ViewBag._utility;
    var _admin = (AdminHelpers)ViewBag._admin;
    var _session = (ISession)ViewBag._session;
    var row = (System.Data.DataRow)ViewBag.itemEdit;
    string jsonRow = ((string)ViewBag.itemJSONEdit) ?? "";
}

@section NavbarBack{
    @await Component.InvokeAsync("AdminNavbarBack", new { linkBack = string.Format("/Admin/{0}", mod.Name) })
}

@await Component.InvokeAsync("AdminBreadcrumb", new { Module = mod })

<script>
var formBuilder;
</script>

<style>
.form-wrap.form-builder .frmb-control li .control-icon
{
    color:blue;
    font-size:16px;
}
</style>

<div class="form-wrapper">
    <form method="post" id="fm_cms" onsubmit="return sConfirmCustom(this);" class=""> 
        @Html.AntiForgeryToken() 
        <div class="row">
            @{
                //string cat_id = _session.GetString("admin_" + mod.Name + "_search_" + mod_config.TableCateField) ?? "0";
                string cat_id = row["cat_id"].ToString() ?? "0";
                <div class="col-12 mb-3">
                    @Html.Raw(
                    _admin.InputSelectDB(
                    mod_config.TableCateLabel,
                    mod_config.TableCateField,
                    cat_id,
                    showAllOption: true,
                    mod_config.TableCate,
                    value_field: "id",
                    text_field: mod_config.TableCateTitle,
                    order_by: mod_config.TableCateOrderby,
                    sort: mod_config.TableCateSort,
                    txtAllOption: "เลือก",
                    ModuleID: mod_config.TableCateModuleID
                    ))

            </div>
                        }
        </div>
        <div class="row"> 
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputText("หัวข้อ <span class='fi fi-th'></span>", "title", (row["title"].ToString() ?? "").Trim()))
            </div>
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputText("หัวข้อ <span class='fi fi-us'></span>", "en_title", (row["en_title"].ToString() ?? "").Trim()))
            </div> 
        </div>

        <div id="section_info">
            <div class="row">
                <fieldset style="
            display: block;
            margin-inline-start: 10px;
            margin-inline-end: 10px;
            padding-block-start: 10px;
            padding-inline-start: 10px;
            padding-inline-end: 10px;
            padding-block-end: 10px;
            min-inline-size: min-content;
            border-width: 1px;
            border-style: solid;
            border-color: #ced4da;
            border-image: initial;
            ">
                    <legend>
                        &nbsp;จัดวางแบบฟอร์ม
                    </legend>
                    <br />
                    <br />
                    <div id="build-wrap" class="buildwrap"></div>
                    <script src="https://cdnjs.cloudflare.com/ajax/libs/jQuery-formBuilder/3.8.3/form-builder.min.js" integrity="sha512-H2n4sb6jgsXLkQpJNWrU8+Z9GNkeFaWnPCW3IACoqBzf4hbjMyEfSRCbSNA671SgiBrjTqJq2Atbmf3/dM6B9w==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
                    <link href="~/css/base/formbuilder.min.css" rel="stylesheet" />
                    <script>
                         
                        @{
                            string final_formData = row["input_element"].ToString();
                            final_formData = System.Text.RegularExpressions.Regex.Replace(final_formData, @"\t|\n|\r", "");
                            final_formData = final_formData.Replace(@"\""", string.Empty); 
                            final_formData = final_formData.Replace(@"\n",""); 
                        }
                        
                        var main_formData = '@Html.Raw(final_formData)';
                          
                        $(document).ready(function () {
                            jQuery(function ($) {
                                var fbEditor = document.getElementById('build-wrap');
                                var options =
                                {
                                    editOnAdd: false,
                                    scrollToFieldOnAdd: false,
                                    showActionButtons: false,
                                    fieldRemoveWarn: false,
                                    disabledAttrs:
                                        [
                                            'access', 'className', 'name', 'multiple'
                                        ],
                                    controlPosition: 'right',
                                    controlOrder:
                                        [
                                            'header',
                                            'paragraph',
                                            'text',
                                            'textarea',
                                            'number',
                                            'date',
                                            'checkbox-group',
                                            'radio-group',
                                            'select',
                                            'file',
                                            'autocomplete'
                                        ],
                                     inputSets:
                                        [ 
                                            {
                                                label: 'Line Break',
                                                name: 'line-break-1',
                                                icon: "-",
                                                showHeader: false,
                                                fields:
                                                [{"type":"header","subtype":"h1","label":"<hr>"}]
                                            },

                                            @{
                                                for (int i = 0; i < ViewBag.EForm_Group_Rows.Rows.Count; i++)
                                                {
                                                    <text>
                                                    {
                                                        label: '<b style="color:blue">@ViewBag.EForm_Group_Rows.Rows[i]["pb_title"]</b>',
                                                        name: 'custom-input-@i',
                                                        icon: "★",
                                                        showHeader: false,
                                                        fields:
                                                        @Html.Raw(ViewBag.EForm_Group_Rows.Rows[i]["pb_input_element"])
                                                    },
                                                    </text>
                                                }
                                            } 
                                        ],  
                                    /*   defaultFields:
                                           [
                                               {
                                                   className: "form-control",
                                                   label: "First Name",
                                                   placeholder: "Enter your first name",
                                                   name: "first-name",
                                                   required: true,
                                                   type: "text"
                                               },
                                               {
                                                   className: "form-control",
                                                   label: "Select",
                                                   name: "select-1454862249997",
                                                   type: "select",
                                                   multiple: "true",
                                                   values:
                                                       [
                                                           {
                                                               label: 'Custom Option 1',
                                                               value: 'test-value'
                                                           },
                                                           {
                                                               label: 'Custom Option 2',
                                                               value: 'test-value-2'
                                                           }
                                                       ]
                                               },
                                               {
                                                   label: "Radio",
                                                   name: "select-1454862249997",
                                                   type: "radio-group"
                                               }
                                           ], */
                                    disableFields: ['button', 'hidden', 'starRating', 'autocomplete' ],
                                    formData: main_formData,
                                }
                                if (typeof fbSanitizeOptions === 'function') fbSanitizeOptions(options);
                                formBuilder = $(fbEditor).formBuilder(options);
                                /*
                                document.getElementById('getXML').addEventListener('click', function () {
                                    alert(formBuilder.actions.getData('xml'));
                                });
                                document.getElementById('getJSON').addEventListener('click', function () {
                                    alert(formBuilder.actions.getData('json'));
                                });
                                document.getElementById('getJS').addEventListener('click', function () {
                                    alert(formBuilder.actions.getData());
                                });
                                */
                            });
                        });
                    </script>
                    <!-- <input type="button" name="a" id="getJSON" /> -->
                </fieldset>
            </div>
        </div>

        <br />

        <div class="row">
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputText("ข้อความ ปุ่มส่งข้อมูล <span class='fi fi-th'></span>", "url", (row["url"].ToString() ?? "").Trim()))
            </div>
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputText("ข้อความ ปุ่มส่งข้อมูล <span class='fi fi-us'></span>", "en_url", (row["en_url"].ToString() ?? "").Trim()))
            </div>
        </div>

        <br />

        <div class="row">
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputText("ข้อความ ขอบคุณ <span class='fi fi-th'></span>", "info2", (row["info2"].ToString() ?? "").Trim()))
            </div>
            <div class="col-lg-6 col-md-6 mb-3">
                @Html.Raw(_admin.InputText("ข้อความ ขอบคุณ <span class='fi fi-us'></span>", "en_info2", (row["en_info2"].ToString() ?? "").Trim()))
            </div>
        </div>

        <br />

        <div id="section_info">
            <div class="row">
                @Html.Raw(_admin.InputTextArea("อีเมล์เจ้าหน้าที่ (ขั้นด้วยเครื่องหมายลูกน้ำ , )", "url_target", (row["url_target"].ToString() ?? "").Trim()))
            </div>
        </div>

        <br />

        <script>
            $(document).ready(function () {
                $(":input[name='issue_date_config']").click(function () {
                    if ($(this).val() == '2') {
                        $('#div_id_issue_date').stop(true, false).slideDown();
                    }
                    else {
                        $('#div_id_issue_date').stop(true, false).slideUp();
                    }
                });
            });
        </script>
        <div class="row">
            <div class="col-lg-6 col-md-6 mb-3">
                <div class="field">
                    <label class="form-label">วันที่แสดงผล</label>
                    <div class="row">
                        <div class="col-md-6 col-sm-6 col-6">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="issue_date_config" id="id_issue_date_config_1" value="1" checked>
                                <label class="form-check-label" for="id_issue_date_config_1">
                                    แสดงตลอดเวลา
                                </label>
                            </div>
                        </div>
                        <div class="col-md-6 col-sm-6 col-6">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="issue_date_config" id="id_issue_date_config_2" value="2">
                                <label class="form-check-label" for="id_issue_date_config_2">
                                    กำหนดเอง
                                </label>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="" id="div_id_issue_date" style="display:none;">
            <div class="row">
                @{
                    string issue_date = ""; try { issue_date = ((DateTime)row["issue_date"]).ToLocalTime().ToString("dd/MM/yyyy HH:mm"); } catch { issue_date = System.DateTime.Now.ToString("dd/MM/yyyy HH:mm"); }
                    string expiry_date = ""; try { expiry_date = ((DateTime)row["expiry_date"]).ToLocalTime().ToString("dd/MM/yyyy HH:mm"); } catch { expiry_date = System.DateTime.Now.AddYears(1).ToString("dd/MM/yyyy HH:mm"); }
                }
                <div class="col-lg-6 col-md-6 mb-3">
                    @Html.Raw(_admin.InputDateTime("วันที่แสดงผล", "issue_date", issue_date))
                </div>
                <div class="col-lg-6 col-md-6 mb-3">
                    @Html.Raw(_admin.InputDateTime("วันที่สิ้นสุดแสดงผล", "expiry_date", expiry_date))
                </div>
            </div>
        </div>

        <input type="hidden" name="input_element" id="id_input_element" value='@Html.Raw(@row["input_element"]).ToString()' />

         
        @await Component.InvokeAsync("AdminBtnSave", new { linkClose = "/Admin/" + mod.Name })
    </form>
</div>

@{
    if ((row["issue_date_config"].ToString() ?? "").Trim() == "2")
    {
        <script>
            document.getElementById('id_issue_date_config_2').checked = true;
            document.getElementById('div_id_issue_date').style.display = '';
        </script>
    }
    else
    {
        <script>
            document.getElementById('id_issue_date_config_1').checked = true;
            document.getElementById('div_id_issue_date').style.display = 'none';
        </script>
    }
}

@section Scripts{
<link rel="stylesheet" href="~/lib/flag-icons-main/css/flag-icons.min.css" />
<script>
    var modConfig = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(mod_config))
    var jsonRow = @Html.Raw(jsonRow)
    function sConfirmCustom(form) {

        document.getElementById('id_input_element').value = formBuilder.actions.getData('json'); 

        if(document.getElementById('id_issue_date_config_2').checked == true)
        { 
            if (
                $('#id_issue_date').val() != '' && $('#id_expiry_date').val() != '' &&
                $('#id_issue_date').datetimepicker('getValue') > $('#id_expiry_date').datetimepicker('getValue')
            ) 
            {
                sAlert("วันที่ไม่ถูกต้อง", "วันที่สิ้นสุด ต้องมากกว่า วันที่แสดงผล", "warning");
                return false;
            }
        }

        // Validate required fields
        var titleField = $('[name="title"]');
        if (titleField.length > 0 && titleField.val().trim() === '') {
            Swal.fire({ icon: 'warning', title: 'กรุณากรอกข้อมูล', text: 'กรุณากรอก Title ก่อนบันทึก', confirmButtonText: 'ตกลง' });
            titleField.focus();
            return false;
        }
        var enTitleField = $('[name="en_title"]');
        if (enTitleField.length > 0 && enTitleField.val().trim() === '') {
            Swal.fire({ icon: 'warning', title: 'กรุณากรอกข้อมูล', text: 'กรุณากรอก EN Title ก่อนบันทึก', confirmButtonText: 'ตกลง' });
            enTitleField.focus();
            return false;
        }
        Swal.fire({
            title: 'ยืนยัน ?',
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#0047B6',
            cancelButtonColor: '#BC1320',
            confirmButtonText: 'ใช่',
            cancelButtonText: 'ปิด',
            showClass: {
                popup: 'animate__animated animate__bounceIn'
            },
            hideClass: {
                popup: 'animate__animated animate__bounceOut'
            }
        }).then((result) => {
            if (result.isConfirmed) {
                form.submit();
            } else {
                return false;
            }
        });
        return false;
    }
    
    $(document).ready(function() {

        checkLockedRow(modConfig, jsonRow);

        
         
        setTimeout(function(){
            $(':input[name="issue_date"]').datetimepicker("destroy");
            $(':input[name="issue_date"]').datetimepicker({
                format: 'd/m/Y H:i',
                minDate: 0
            });
        },500);
    });
</script>
}